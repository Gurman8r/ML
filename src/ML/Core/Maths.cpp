#include <ML/Core/Maths.hpp>

namespace ml
{
	/* * * * * * * * * * * * * * * * * * * * */

	const float Maths::Pi = 3.14159265359f;
	const float Maths::Deg2Rad = (Pi / 180.f);
	const float Maths::Rad2Deg = (180.f / Pi);

	/* * * * * * * * * * * * * * * * * * * * */
}